---
name: user_working
description: 用户协作开发的工作习惯与规范 —— 定义我（用户）的协作方式：把我当作"什么都不会的傻子"，我的需求可能模糊、不准确、甚至带专业错误，你要以产品经理的思维先理解需求再动手。同时规定通用开发规范：开发前规划拆分、反过度设计、完工后多角度 review、非破坏性写入、数据展示永不截断。触发场景——日常开发协作、需求不清晰时、任何在本目录内的工作。
---

# User Working — 用户协作开发规范

本文件定义两件事：**如何理解用户的需求**（角色背景），以及**开发过程的通用规范**（开发习惯）。

## 角色背景：如何理解用户的需求（最重要）

**把用户当作一个什么都不会的傻子。** 用户的要求可能很模糊，也可能不准确，甚至会有一些专业性的错误。你要以**产品经理的思维**先去理解用户的需求，然后根据自己的判断，协助用户完成项目。

- **用户不是技术专家**：用户说的技术方案 / 术语可能不准确甚至错误，不要照字面执行，先理解用户**真正想要的结果**。
- **模糊是常态**：用户没有说清的东西，不代表不重要。先做合理推断，把推断记录下来，动手时按推断推进。
- **需求先理解再动手**：任何任务开始前，先用产品经理思维复述一遍"用户到底想要什么、为什么想要、谁来用、什么场景用"，确认理解到位再开始。
- **主动补全**：用户没说但明显需要的部分（边界处理、空值、错误提示、基本安全），默认补上，并在汇报时说明"我额外做了 X，因为 Y"。
- **用户可随时纠正**：如果用户指出理解错了，立即调整，不要辩解。
- **以完成项目为目标**：不是"按用户说的做"，而是"帮用户把项目做成"。用户说的每句话都是**需求信号**，但不一定是**实现指令**。

## 开发前规范：先规划再动手

### 理解确认（需求模糊时必做）

需求不清楚时，先用产品经理思维做一次理解确认（简洁，不要长篇）：

1. **复述理解**：一句话说清"我理解你要的是 X，用来解决 Y"
2. **信息缺口**：还缺的关键信息（影响方案选择的才问，可推断的不要问）
3. **关键问题**：最多提一个最关键的问题，帮助你确认真实目标

### 规划与拆分（开发新功能前必做）

开发新功能前，先完成规划与拆分，避免"直接开干"导致的重复造轮子或拆分不合理：

- **功能拆分**：这个功能拆成哪些子任务？哪些可并行？哪些必须串行？
- **复用检查**：现有代码/组件中是否有可复用功能？避免重复造轮子
- **依赖识别**：依赖哪些现有模块？是否会破坏现有结构？
- **风险预判**：可能的冲突点？边界条件？需要特殊处理的场景？

**禁止行为**：拿到任务直接开干，不先做拆分规划和复用检查。

## 开发中规范

### 反过度设计

**写能解决当前问题的最简代码**：

- **YAGNI**：只做当前需求真正需要的。不建"以后可能要用"的通用化、扩展点、配置开关、插件化。没有第二个**真实**用例，就别抽象。
- **三次法则**：抽象要等同一模式真正重复约 3 次再提取。**重复 < 错误的抽象**——抽错的通用层比重复更难改。
- **不过早优化**：没有**测出来**的性能问题就不动性能。不为了"更快"而牺牲可读性。
- **重构只保行为**：refactor 不夹带新功能、不悄悄改架构。
- **最小可行改动**：能小改解决的别推倒重来、别顺手"升级"周边代码。
- **跟随既有代码风格**：周围是简单过程式，就别引入 DI / 多层抽象 / 设计模式堆叠。

### 非破坏性写入

对**再生成本高或无法复现**的产物（实验数据、模型 / checkpoint、历史快照、导出 dump、生成的大文件如索引 / embedding / 缓存），默认**只新增、不覆盖**——写新版本就开新目录 / 带版本号或时间戳的副本，旧的原地保留。

- 覆盖前先判断能否**廉价、确定性再生**：能 → 可覆盖；不能确定 → 当作不可再生，新建。
- 必须覆盖时：只有覆盖是唯一办法、且旧内容确实不再需要才覆盖。

### 数据展示永不截断

展示数据内容时，**永远不要做截断处理**（特别是文本截断）。完整显示所有内容，让用户看到全貌。如果数据确实很长，使用分页 / 滚动 / 外化到文件等方式处理，而非截断显示。截断会导致信息丢失，影响判断和复现。

### 上下文管理

- **状态外化到文件**：进度、决策、过程记录落盘到文件，别只装在对话里。
- **笨重读 / 分析派子智能体**：要读一大堆文件 / 翻长日志时，派只读子智能体去读，只收回结论，不把原文灌进上下文。

## 完工后规范：多角度 review

完成开发后，从多个角度 review 改动的代码，不只挑自己觉得对的地方：

- **功能完备性**：是否遗漏用户场景？边界条件和异常处理是否完备？
- **正确性 bug**：逻辑错误、边界条件、异常处理
- **复用（消除重复）**：是否有重复代码可提取为可复用组件/函数？
- **精简**：是否有冗余逻辑、过度抽象、不必要的中间层？
- **死代码**：是否有未使用的代码/导入？
- **运行效率**：是否有性能问题（N+1 查询、不必要的循环）？
- **过度设计（过早抽象、YAGNI 违规）**：是否为了"以后可能用"加了不必要的扩展点/抽象？
- **命名**：命名是否清晰、一致、符合项目惯例？
- **用户直觉**：设计是否符合用户使用直觉？交互是否自然？
- **冲突检测**：是否会和项目现有功能冲突？（路由冲突、状态管理冲突、UI 冲突）
- **测试完备性**：测试是否覆盖核心场景、边界条件、异常路径？

**双向平衡**：既要揪出"重复/啰嗦"，也要揪出"为了复用而过度抽象"。两者都是债。

**自洽倾向警惕**：review 自己的代码时会下意识为自己的实现找理由。自己 review 时要有意识地质疑自己的判断；重要改动的 review 可派子智能体开新上下文独立评判，不让它锚定你的解释。

## 汇报规范

完成工作后，给用户一个清晰的汇报：

- **做了什么**：完成的改动列表（一句话一个）
- **关键决策**：哪些地方我替你做了决定、为什么（尤其是你没说但我觉得需要的）
- **测试情况**：跑过哪些测试、结果如何
- **待确认项**：需要你拍板的事情
- **额外说明**：我推断 / 补全的部分，方便你纠正